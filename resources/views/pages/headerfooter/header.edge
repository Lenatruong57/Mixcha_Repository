<header class="site-header">
  <div class="container site-header__inner">
    <a class="site-header__brand" href="/">
      <img src="/images/logo.png" alt="Mixcha Logo">
    </a>

    <nav class="site-nav" aria-label="Hauptnavigation">
      <a href="/produkte">Unsere Produkte</a>
      <a href="/blog">Blog</a>

      
      @if(session.get('adminId'))
        <a href="/admin" class="nav-icon" aria-label="Adminbereich">
          <img src="/images/iconlogin.png" alt="Admin">
        </a>

        <form method="POST" action="/haendler-logout" class="nav-icon-form">
          {{ csrfField() }}
          <button type="submit" class="nav-icon" aria-label="Logout">
            <img src="/images/iconlogout.png" alt="Logout">
          </button>
        </form>

     
      @elseif(session.get('customerId'))
        <a href="/profil" class="nav-icon" aria-label="Profil">
          <img src="/images/iconlogin.png" alt="Profil">
        </a>

        <form method="POST" action="/logout" class="nav-icon-form">
          {{ csrfField() }}
          <button type="submit" class="nav-icon" aria-label="Logout">
            <img src="/images/iconlogout.png" alt="Logout">
          </button>
        </form>

    
      @else
        <a href="/login" class="nav-icon" aria-label="Login">
          <img src="/images/iconlogin.png" alt="Login">
        </a>
      @endif

  
      <a href="/warenkorb" class="nav-icon nav-cart" aria-label="Warenkorb">
        <img src="/images/iconwarenkorb.png" alt="Warenkorb">
        @if((cartCount || 0) > 0)
          <span class="nav-cart__badge">{{ cartCount }}</span>
        @endif
      </a>
    </nav>
  </div>
</header>